<!-- Validation failure messages -->
<% if @country.errors.any? %>
  <% @country.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Country</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_country" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for country_name -->
      <div class="form-group">
        <label for="country_name" class="control-label">
          Country name
        </label>

        <input type="text" id="country_name" name="country_name" placeholder="Country name" class="form-control" value="<%= @country.country_name %>">
      </div>

      <button class="btn btn-success">
        Create Country
      </button>
      or
      <a href="/countries">Cancel</a>
    </form>
  </div>
</div>
